A repository I am using to learn and test Docker with MySQL and maven

To run locally:
  - git clone https://github.com/Jackson-Wozniak/docker-maven-spring-test.git
  - mvn clean package -DskipTests
  - docker-compose up
